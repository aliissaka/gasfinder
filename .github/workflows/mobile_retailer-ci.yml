name: mobile_retailer-ci

on:
  push:
    branches: [main]
    paths:
      - "mobile_retailer/**"
      - "shared_flutter/**"
      - ".github/workflows/mobile_retailer-ci.yml"
  pull_request:
    paths:
      - "mobile_retailer/**"
      - "shared_flutter/**"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile_retailer
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.32.7"
          channel: stable
          cache: true

      - run: flutter pub get
      - run: flutter analyze
      - run: flutter test
      - run: flutter build apk --release --split-per-abi
